@AGENTS.md

## Claude Code

- The machine-global baseline (engineering judgment, git identity, OS/runtime) is installed separately at `~/.claude/CLAUDE.md` and is not part of this repo. This file covers only this project.
- Scope work with `pnpm --filter <pkg>` instead of running the whole monorepo when you only touched one workspace.
